<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <meta name="og:title" content="{{ render.metadata.content_name }}">
    <meta name="og:author" content="{{ render.metadata.author_name }}">
    <link rel="stylesheet" type="text/css" href="{{ base_url }}/css/style.css">
    <title>
        {%- if render.metadata.content_name is defined -%}{{ render.metadata.content_name ~ " | " }}{% endif -%}{{ site_title -}}
    </title>
</head>